# External contracts

Add versioned execution graph, event, evidence, and configuration schemas only after the relevant ADR is accepted. Validate external data at boundaries. No product schema is implemented yet.
