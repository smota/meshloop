---
name: meshloop-roles
description: List bundled Meshloop roles. Slash /meshloop:roles.
---

# /meshloop:roles

```text
meshloop roles --json
```

Ids are always `meshloop:origin|planner|scout|worker|reviewer`.
